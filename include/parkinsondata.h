#include "common.h"
//Read the parkinsons.data file and set the parameters in a matrix. The somConfig n and p will be updated with the size of the file
dataVector* getParkinsonsData(somConfig *config);
